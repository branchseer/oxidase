class A {
    constructor(public a) {
        // rewind
        function aa() {
            foo<{ a: () => (x<T>) + y}>(1);
        }

        function bb() {
            foo<{ a: () => (x<T>) & y}>(1);
        }

        super();

        function cc() {
            const bar = (a = () => 1 as number);
        }
        const baz = (a = () => 1 as number) => 1;
    }
}
